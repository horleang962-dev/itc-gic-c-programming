#include "rectangle.h"

// Function to calculate the perimeter: (width + height) * 2

double calculatePerimeter(double width, double height) {
    return (width + height) * 2.0;
}

// Function to calculate the area: width * height

double calculateArea(double width, double height) {
    return width * height;
}